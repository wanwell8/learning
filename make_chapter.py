# -*- coding: utf-8 -*-
from build_docx import (TITLE, AUTH, H1, H2, H3, P, EQ, CAP, REFTITLE, REF,
                        three_line_table, write_docx, para, run)

parts = []

# ===== 标题 / 作者 =====
parts.append(TITLE('算法引擎与博弈优化（模型求解层）'))
parts.append(TITLE('——面向水光储基地无功支撑与分阶段快速调压的'))
parts.append(TITLE('强化学习与主从博弈方法'))
parts.append(AUTH('（本章为研究报告／学位论文的模型求解层章节，格式参照《电力系统自动化》）'))

# ===== 摘要 =====
parts.append(para(
    run('摘    要：', bold=True, sz=21, ea='黑体', ascii_='SimHei') +
    run('在以水电、光伏、储能（水光储）为主体的新能源基地中，三类电源的无功调节能力'
        '随运行工况动态变化，其整体无功支撑能力构成了调压调度的可行域边界。如何在故障'
        '持续与电压恢复两个阶段内，将这一时变能力边界转化为安全、快速且经济的无功优化'
        '调度决策，是提升系统电压稳定性的关键。本章聚焦模型求解层，系统梳理深度强化学习'
        '（DRL）在无功/电压控制中的建模范式与安全约束机制，以及主从博弈（Stackelberg）'
        '与领导者—演员（Leader–Actor）多智能体架构在异构资源协调中的作用机理；在此基础上，'
        '结合水光储基地的物理特征，提出三项可落地的算法创新：运行工况自适应的无功能力'
        '边界安全集、故障—恢复分阶段双时间尺度迁移强化学习、以及基于无功裕度影子价格的'
        '主从博弈—异步多智能体协调框架，并给出统一求解框架与技术路线。', sz=21),
    indent_chars=2, align='both'))
parts.append(para(
    run('关键词：', bold=True, sz=21, ea='黑体', ascii_='SimHei') +
    run('水光储基地；无功支撑能力；电压稳定；深度强化学习；安全强化学习；'
        '主从博弈；Stackelberg-Nash 均衡；多智能体；分阶段调压', sz=21),
    indent_chars=2, align='both'))

# ===== 2.1 引言 =====
parts.append(H1('2.1', '模型求解层的定位与求解需求'))
parts.append(P('能力边界量化层给出了"在给定运行工况下，水光储基地能提供多少无功"这一可行域，'
    '而调度决策的产生则取决于模型求解层。本章的核心任务是：把第一阶段量化得到的、随有功'
    '出力、荷电状态（SOC）、并网点电压和故障状态而变化的时变无功能力边界，作为约束输入，'
    '在故障持续阶段与电压恢复阶段分别生成快速、安全且经济的无功优化调度策略，从而为提升'
    '系统电压稳定性提供方法支撑。'))
parts.append(P('面向水光储基地的无功/电压协调求解具有以下难点：①高维、强耦合且时变——三类电源'
    '数量多、调节特性各异，网络潮流非凸；②强不确定性——光照、来水与负荷波动，故障扰动随机；'
    '③快速性要求——故障持续阶段需要毫秒至秒级的动态电压支撑，传统逐时优化难以满足；'
    '④主体异构与信息有限——光伏、储能电站常隶属不同主体，模型与目标各异、不愿完全共享私有'
    '信息，呈非合作博弈特征。针对上述难点，本章从两条技术主线展开：其一为强化学习在无功/'
    '电压控制中的应用（DRL for Volt-VAR），解决"模型不精确、需在线快速决策"的问题；其二为'
    '主从博弈与领导者—演员架构（Stackelberg Game & Actor–Critic），解决"多异构主体分层协调'
    '与非合作均衡"的问题。两条主线最终融合为一个统一的求解框架。'))

# ===== 2.2 DRL for Volt-VAR =====
parts.append(H1('2.2', '强化学习在无功/电压控制中的应用'))

parts.append(H2('2.2.1', '无功/电压控制的序贯决策建模范式'))
parts.append(P('强化学习将无功/电压控制视为智能体与配电/送出系统环境交互的序贯决策问题，'
    '常用马尔可夫决策过程（MDP）描述，由五元组 (S, A, P, R, γ) 定义：状态 S 包含各节点电压、'
    '三类电源的有功/无功出力、储能 SOC、光照/来水预测、并网点状态及故障标志；动作 A 为可调'
    '设备的控制量，如逆变器无功设定值、储能四象限无功、水电机组励磁/调相、有载调压变压器'
    '（OLTC）分接头与电容器组（CB）投切；奖励 R 通常综合电压偏差、网损与运行/弃用成本；'
    'γ∈(0,1) 为折扣因子。其优化目标为最大化期望折扣累计奖励：'))
parts.append(EQ('max_π  E[ Σ_t γ^t · R(s_t, a_t) ]', '2-1'))
parts.append(P('当含有多个独立决策的电源/区域时，问题进一步建模为马尔可夫博弈（Markov Game，'
    '亦称随机博弈）；当电压越限等安全条件必须以高概率满足时，则建模为带约束的马尔可夫决策'
    '过程（CMDP）。文献中既有基于动作值（如 Q 学习、Dueling DQN）的离散动作方法，也有基于'
    '策略梯度（如 SAC、PPO、DDPG）的连续动作方法。'))

parts.append(H2('2.2.2', '安全强化学习与能力边界硬约束（CMDP / 约束软演员—评论家）'))
parts.append(P('无功/电压控制对安全性要求极高：探索性动作若引起电压越限，可能损坏设备、危及'
    '系统稳定，这在真实电网中不可接受。文献[1]将 Volt-VAR 控制建模为 CMDP，并提出约束软'
    '演员—评论家（Constrained Soft Actor–Critic, CSAC）算法：以网损/电压偏差作为奖励，'
    '以电压越限次数作为代价函数（cost），通过拉格朗日乘子将安全约束嵌入策略优化，在带约束'
    '的最大熵框架下迭代更新策略网络、双 Q 评论家网络与乘子，收敛到局部最优且可行的策略。'
    '其代价（约束）与带约束目标可写为：'))
parts.append(EQ('c_t = Σ_i [ 1(V_i,t > V_max) + 1(V_i,t < V_min) ]', '2-2'))
parts.append(EQ('max_π  E[ Σ_t γ^t ( R_t + α·H(π(·|s_t)) ) ]   s.t.  E[ Σ_t γ^t c_t ] ≤ d', '2-3'))
parts.append(P('式中：1(·) 为示性函数；H(·) 为策略熵，鼓励探索；α 为熵温度；d 为允许的约束预算。'
    '由于 CSAC 是离策略（off-policy）方法，可复用历史运行数据，样本效率与约束满足能力均优于'
    '约束策略优化（CPO）等在策略方法。这一"奖励—代价"分离的安全机制，为后文把无功能力边界'
    '作为硬约束嵌入提供了直接范式。'))

parts.append(H2('2.2.3', '双时间尺度协同与离线—在线迁移学习'))
parts.append(P('为兼顾慢动作的常规无功设备与快动作的电力电子资源，文献[3]提出两时间尺度混合'
    '电压控制：慢时间尺度将电容器组、OLTC 与储能的有功/无功优化建模为混合整数二阶锥规划'
    '（MISOCP）求解；快时间尺度由多智能体强化学习（MARL）控制智能逆变器无功，提出带注意力'
    '机制的经验增强多智能体演员—评论家（EA-MAAC），采用集中训练—分散执行（CTDE）框架以兼顾'
    '协调性与在线快速性。针对模型与真实系统之间的"迁移鸿沟"，文献[2]提出两阶段 DRL：离线'
    '阶段（OFF-A）利用近似模型与历史经验预训练，在线阶段（ON-A）以模型无关方式运行并采用'
    'SAC 进行微调，借助 SAC 的离策略特性把离线策略安全迁移到在线，显著降低真实系统上的'
    '训练风险与样本需求。'))
parts.append(P('上述"慢/快两时间尺度"与"离线/在线两阶段"思想，恰好与本研究"故障持续—电压恢复"'
    '的分阶段调压需求高度契合，是后续创新点的方法基础。'))

parts.append(H2('2.2.4', '面向水光储基地的应用创新'))
parts.append(H3('（1）', '创新点一：运行工况自适应的无功能力边界安全集'))
parts.append(P('已有安全强化学习多以静态动作上下限或电压越限作为约束[1]。本研究的创新在于：'
    '将第一阶段量化得到的、随运行工况动态变化的三类电源无功能力边界，直接构造为强化学习的'
    '"安全动作集"，并通过动作投影/掩码层保证每一步动作严格落在可行域内。对逆变型电源'
    '（光伏、储能），其无功受容量圆约束；对水电同步机，其无功受定子电流、转子励磁电流与'
    '静态稳定极限围成的 P-Q 能力曲线约束：'))
parts.append(EQ('Q_i ∈ [ Q_i^min(P_i, V_i, SOC_i),  Q_i^max(P_i, V_i, SOC_i) ]', '2-4'))
parts.append(EQ('逆变型(光伏/储能):  |Q_i| ≤ sqrt( S_i,N^2 − P_i^2 )', '2-5'))
parts.append(EQ('水电同步机:  Q_i ∈ Ω_PQ(I_stator^max, I_field^max, 稳定极限)', '2-6'))
parts.append(P('式中：S_i,N 为变流器额定容量。由此把"能力边界量化"无缝转化为求解层的安全层：'
    'CMDP 的代价函数在式(2-2)电压越限的基础上增加能力边界越界项，动作在执行前经可行域投影'
    '校正。该机制使智能体在保证不突破各电源物理无功能力的前提下学习调压策略，从根本上消除'
    '"探索导致越界"的风险，并使量化得到的整体无功支撑能力成为可调度的真实裕度输入。'))

parts.append(H3('（2）', '创新点二：故障—恢复分阶段双时间尺度迁移强化学习'))
parts.append(P('借鉴两时间尺度[3]与两阶段迁移[2]思想，本研究将"两阶段"重新映射为电压响应的'
    '两个物理阶段，构成分阶段快速调压调度：'))
parts.append(P('① 故障持续阶段（快时间尺度，毫秒—秒级）：以模型无关的分散式 DRL 控制光伏与储能'
    '逆变器，进行紧急动态无功注入与暂态电压支撑，强调速度与就地自治；离线阶段在含能力边界的'
    '近似基地模型上预训练，缓解故障样本稀缺问题，在线再行迁移微调。', indent=2))
parts.append(P('② 电压恢复阶段（慢时间尺度，分钟级）：以模型驱动的无功优化（MISOCP）协调水电'
    '励磁/调相、OLTC、电容器组并复位逆变器设定值，在恢复电压的同时避免过调与二次电压问题，'
    '兼顾经济性与稳态安全。', indent=2))
parts.append(P('两阶段之间设置基于电压跌落深度与电压变化率（dV/dt）的阶段切换触发机制，在快、慢'
    '控制回路间交接控制权；而创新点一得到的时变无功裕度，则同时作为两个阶段优化的可行域输入。'
    '相比传统单一时间尺度方案，该结构既满足故障阶段的快速性，又保证恢复阶段的协调最优。'))

# ===== 2.3 Stackelberg & Actor-Critic =====
parts.append(H1('2.3', '主从博弈与领导者—演员架构'))

parts.append(H2('2.3.1', '主从博弈（Stackelberg）建模'))
parts.append(P('当无功调度涉及隶属不同主体、目标各异且不愿共享私有模型的多个电源时，集中式优化'
    '难以适用，分层非合作博弈更为贴切。文献[5]将配电公司（Disco，领导者）与需求响应聚合商'
    '（DRA，跟随者）之间的层级交互建模为一主一从 Stackelberg 博弈：领导者先决策（如零售电价），'
    '跟随者在观测到领导者策略后优化自身效用；其创新在于不将双层博弈规约为单层问题，而以'
    'Dueling DQN 直接在双层结构中求解，从而保护跟随者私有模型、并克服 DQN 的过估计问题。'
    'Stackelberg 双层博弈的一般形式为：'))
parts.append(EQ('上层(领导者):  max_x  U_L( x, y*(x) )', '2-7'))
parts.append(EQ('下层(跟随者):  y*(x) = arg max_y  U_F( y, x )', '2-8'))

parts.append(H2('2.3.2', 'Stackelberg-Nash 均衡与多智能体演员—评论家'))
parts.append(P('当跟随者为多个且彼此竞争时，下层退化为 Nash 博弈，整体构成 Stackelberg-Nash '
    '动态博弈。文献[4]提出异步多智能体框架与 Stackelberg-Nash 多智能体近端策略优化算法'
    '（SN-MAPPO）：上层效用公司为领导者，多个消费者为相互非合作的跟随者，各智能体以演员—'
    '评论家（actor–critic, PPO）学习策略，并被证明可收敛到 Stackelberg-Nash 均衡；该框架'
    '还显式刻画了参与者动作的时序关系，弥补了传统需求响应忽视主体利益与时序耦合的不足。'
    '均衡条件可表述为：领导者在预判跟随者最优响应的前提下最优，且任一跟随者在其余跟随者'
    '与领导者策略给定时无单方面改进激励：'))
parts.append(EQ('U_F^k( y_k*, y_{−k}*, x* ) ≥ U_F^k( y_k, y_{−k}*, x* ),  ∀k, ∀y_k', '2-9'))

parts.append(H2('2.3.3', '异步多智能体与领导者—演员架构'))
parts.append(P('"领导者—演员"架构由一个上层领导者智能体与多个下层演员（跟随者）智能体组成：'
    '领导者基于全局态势输出协调信号（基于值的 Dueling DQN[5] 或基于策略的网络），各演员智能体'
    '将本地观测映射为本地控制设定值，并以评论家进行价值评估。由于不同主体响应速度迥异，'
    '采用异步更新[4]可自然刻画多时间尺度交互，避免强制同步带来的效率损失。'))

parts.append(H2('2.3.4', '面向水光储基地的应用创新'))
parts.append(H3('（3）', '创新点三：基于无功裕度影子价格的主从博弈—异步多智能体协调'))
parts.append(P('本研究将主从博弈与领导者—演员架构迁移到水光储基地的无功协调，提出第三项创新：'
    '以基地调度中心（或送出侧）作为领导者，其不再下发电价，而是下发"无功裕度影子价格/电压'
    '目标包络"这一协调信号；水电、光伏、储能三类电源（或其场站控制器）作为相互非合作的'
    '跟随者演员，在各自由创新点一确定的能力边界内优化自身无功出力，兼顾调压贡献与自身代价'
    '（弃光成本、电池退化、机组损耗）。领导者在预判三类跟随者最优响应的基础上优化全局电压'
    '稳定与经济性，整体收敛到 Stackelberg-Nash 均衡：'))
parts.append(EQ('领导者:  max_{λ}  W( 电压稳定裕度, 网损, 协调成本 ;  Q*(λ) )', '2-10'))
parts.append(EQ('跟随者k:  Q_k*(λ) = arg max_{Q_k∈[Q_k^min,Q_k^max]} [ λ·ΔV_k(Q_k) − C_k(Q_k) ]', '2-11'))
parts.append(P('式中：λ 为领导者下发的无功裕度影子价格；ΔV_k(·) 为电源 k 的调压贡献；C_k(·) 为其'
    '无功调节代价。该机制有三重优势：①保护各场站私有模型与商业信息（双层不规约求解[5]）；'
    '②天然适配水电（慢、连续大容量）、光伏（快、受光照与容量圆限制）、储能（快、受 SOC 限制）'
    '响应速度差异，以异步多智能体[4]建模；③把分散自利的电源行为引导至全局电压稳定目标，'
    '为分阶段调压提供可信的协调闭环。'))

# ===== 2.4 综合 =====
parts.append(H1('2.4', '创新点综合与统一求解框架'))
parts.append(P('综合 2.2、2.3 的方法基础，本研究形成"能力边界感知—博弈协调—分阶段决策"三位一体的'
    '统一求解框架：以创新点一构造时变无功能力边界安全集作为可行域；以创新点三的主从博弈—'
    '领导者—演员架构实现三类异构电源的分层非合作协调；以创新点二的故障—恢复分阶段双时间尺度'
    '结构组织求解时序，最终输出兼顾快速性、安全性与经济性的无功优化调度策略。各文献算法与'
    '本研究创新点的对应关系见表 1。'))

parts.append(CAP('表1  文献算法与本研究创新点的对应关系'))
parts.append(three_line_table(
    ['文献/算法', '核心方法', '原应用场景', '本研究的迁移与创新'],
    [
        ['文献[1] CSAC', '约束软演员—评论家 / CMDP', '配电网 Volt-VAR 安全控制', '创新点一：以时变无功能力边界为安全集与硬约束'],
        ['文献[3] EA-MAAC', '两时间尺度 MISOCP+MARL（CTDE，注意力）', '有功/无功协调电压控制', '创新点二：映射为故障/恢复双时间尺度回路'],
        ['文献[2] 两阶段DRL', '离线预训练+在线SAC迁移', '逆变器 Volt-VAR', '创新点二：缓解故障样本稀缺、安全迁移'],
        ['文献[5] Dueling DQN', '一主一从 Stackelberg 双层不规约求解', '需求响应资源交易', '创新点三：电价→无功裕度影子价格'],
        ['文献[4] SN-MAPPO', 'Stackelberg-Nash 异步多智能体演员—评论家', '双层非合作需求响应', '创新点三：异步刻画水/光/储响应速度差异'],
    ],
    [1900, 2600, 2200, 2326]))

parts.append(CAP('图1  面向水光储基地的统一求解框架（能力边界—博弈协调—分阶段决策）'))
parts.append(P('（图1 说明）框架自下而上分三层：底层为能力边界量化层，输出随工况变化的三类电源'
    '无功能力边界；中层为博弈协调层，调度中心作为领导者下发无功裕度影子价格，水/光/储跟随者'
    '演员在能力边界内异步优化无功出力，收敛到 Stackelberg-Nash 均衡；上层为分阶段决策层，'
    '由电压跌落/dV-dt 触发器在故障持续阶段（快、分散 DRL）与电压恢复阶段（慢、MISOCP 协调）'
    '间切换，能力边界贯穿两阶段作为可行域约束。', indent=2))

# ===== 2.5 小结 =====
parts.append(H1('2.5', '本章小结'))
parts.append(P('本章从模型求解层出发，系统论述了深度强化学习在无功/电压控制中的建模范式与安全'
    '约束机制（MDP/CMDP/马尔可夫博弈、CSAC、两时间尺度与离线—在线迁移），以及主从博弈与'
    '领导者—演员架构（Stackelberg 双层不规约求解、Stackelberg-Nash 均衡、异步多智能体演员—'
    '评论家）的作用机理。在此基础上，面向水光储基地无功支撑与分阶段快速调压的任务，提出三项'
    '创新：①运行工况自适应的无功能力边界安全集，把能力量化结果转化为强化学习硬约束；②故障—'
    '恢复分阶段双时间尺度迁移强化学习，兼顾故障阶段快速性与恢复阶段协调最优；③基于无功裕度'
    '影子价格的主从博弈—异步多智能体协调，实现三类异构、自利电源在保护私有信息前提下的分层'
    '协调。三者融合为统一求解框架，为后续分阶段调压调度策略与无功优化调度机制、进而为提升'
    '系统电压稳定性提供了算法引擎与方法基础。'))

# ===== 参考文献 =====
parts.append(REFTITLE())
parts.append(REF('[1] WANG W, YU N, GAO Y, et al. Safe Off-Policy Deep Reinforcement Learning '
    'Algorithm for Volt-VAR Control in Power Distribution Systems[J]. IEEE Transactions on '
    'Smart Grid, 2020, 11(4): 3008-3018.'))
parts.append(REF('[2] LIU H, WU W. Two-Stage Deep Reinforcement Learning for Inverter-Based '
    'Volt-VAR Control in Active Distribution Networks[J]. IEEE Transactions on Smart Grid, '
    '2021, 12(3): 2037-2047.'))
parts.append(REF('[3] HU D, YE Z, GAO Y, et al. Multi-Agent Deep Reinforcement Learning for '
    'Voltage Control With Coordinated Active and Reactive Power Optimization[J]. IEEE '
    'Transactions on Smart Grid, 2022, 13(6): 4873-4886.'))
parts.append(REF('[4] NIE Y, LIU J, LIU X, et al. Asynchronous Multi-Agent Reinforcement '
    'Learning-Based Framework for Bi-Level Noncooperative Game-Theoretic Demand Response[J]. '
    'IEEE Transactions on Smart Grid, 2024, 15(6): 5622-5637.'))
parts.append(REF('[5] ZHANG Y, AI Q, et al. Intelligent Demand Response Resource Trading '
    'Using Deep Reinforcement Learning[J]. CSEE Journal of Power and Energy Systems, 2024, '
    '10(6): 2622-2636.'))

write_docx('/home/user/learning/算法引擎与博弈优化_模型求解层.docx', parts)
